version: '3.8'

services:
  app:
    build: .
    ports:
      - "3100:3100"
    environment:
      - CLAUDE_DIR=/data/.claude
    volumes:
      - ~/.claude:/data/.claude:ro
    restart: unless-stopped
