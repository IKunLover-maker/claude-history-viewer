version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - CLAUDE_DIR=/data/.claude
    volumes:
      - ~/.claude:/data/.claude:ro
    restart: unless-stopped
